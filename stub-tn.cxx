#include <cmath>
#include <cstring>
#include <iostream>
#include <utility>
#include <vector>

using namespace std;

typedef uint64_t Int;

Int solve(Int N) {
  return 0;
}

int main() {
  int T;
  cin >> T;
  
  for (int i = 0; i < T; ++i) {
    Int N;
    cin >> N;
    cout << solve(N) << endl;
  }
  
  return 0;
}
